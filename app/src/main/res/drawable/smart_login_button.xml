<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@color/smart_login_button_background" />

    <!-- Corner radius -->
    <corners android:radius="8dp" />

    <!-- Shadow -->
    <item>
        <shape>
            <solid android:color="@color/smart_login_button_shadow_color" />
            <corners android:radius="8dp" />
        </shape>
    </item>

</shape>
